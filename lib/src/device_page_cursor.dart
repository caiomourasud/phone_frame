/// A phone has no cursor to take away. This side of `page_cursor.dart` exists so that the widget
/// which asks for it compiles for the phones as well.
void hidePageCursor({required bool hidden}) {}
